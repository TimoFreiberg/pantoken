{
  "$schema": "../target/schemas/desktop-schema.json",
  "identifier": "remote-commands",
  "description": "Grants the hub-served web client at http://127.0.0.1:<port> access to the host-management Tauri commands (profile CRUD, ensure_remote_host, host_state, list_hosts, disconnect_host), so the in-app host picker and Settings can manage and drive remote computer connections. Same narrow-exception pattern as dock-attention.json and window-drag.json.",
  "local": false,
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*/**"]
  },
  "permissions": ["allow-remote-commands"]
}
